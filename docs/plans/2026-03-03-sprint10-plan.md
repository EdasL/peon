# Sprint 10 — Bug Fixes + UX Polish

## Tasks

### 1. Fix P0 bugs
- Add missing `type Step` declaration in OnboardingPage.tsx
- Add `componentDidCatch` to ErrorBoundary
- Add catch block to `launch()` in OnboardingPage

### 2. SSE reconnect re-fetches chat history (P1-1)
- Re-fetch chat history on SSE reconnect to fill gaps from disconnections
- Files: `packages/web/src/hooks/use-chat.ts`

### 3. Board fixes (P1-3, P1-4, P2-5)
- Fix columnMap rollback on moveTask failure
- Remove Board's own h-screen + header (embedded in ProjectPage already)
- Fix column scroll height to use flex instead of viewport calc
- Files: `packages/web/src/hooks/use-board.ts`, `packages/web/src/components/board/Board.tsx`, `packages/web/src/components/board/Column.tsx`

### 4. Small UX fixes (P1-6, P1-8, P2-4, P2-10)
- Provisioning overlay: don't show "Ready!" for stopped status
- Rename onBlur: skip PATCH if name unchanged
- Chat: support Shift+Enter for multiline
- Add 404 catch-all route
- Files: multiple
