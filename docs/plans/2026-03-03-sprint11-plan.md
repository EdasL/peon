# Sprint 11 — UX Polish Pass

## Tasks

### 1. Add settings/logout to ProjectPage header
Users can't access settings or sign out from the project page without going back to dashboard.
- Files: `packages/web/src/pages/ProjectPage.tsx`

### 2. Activity feed scroll guard
Feed auto-scrolls to top on every new event, even when user is reading older events.
- Files: `packages/web/src/components/project/ActivityFeed.tsx`

### 3. Fix streaming cursor position
Chat streaming cursor renders as a separate block below markdown content instead of inline.
- Files: `packages/web/src/components/chat/ChatPanel.tsx`

### 4. Dashboard running project affordance
Running vs stopped project cards look identical — no visual hint that running opens chat vs navigates.
- Files: `packages/web/src/pages/DashboardPage.tsx`
